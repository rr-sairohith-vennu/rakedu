# Deployables

Deployables registered in this repository. Each deployable has a canonical directory
at `.forge/deployables/<name>/` created by `forge:deployable` during discovery.

| Deployable | Description | Status |
|-----------|-------------|--------|
